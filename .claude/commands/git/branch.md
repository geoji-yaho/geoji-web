---
description: 새 브랜치를 생성하고 전환해주세요.
---

# Branch

`develop`에서 `feature/{슬러그}`를 따서 전환한다. 절차는 `.agents/skills/geoji-git/SKILL.md` 브랜치 전략 절이고 금지 패턴은 `.agents/rules/git-workflow.md`다. 이름을 인자로 받았으면 그대로 쓰고 없으면 현재 작업 맥락에서 제안해 확인받는다. 끝나면 `git branch --show-current`로 결과를 확인한다.
