# AGENTS.md

거지방 AI 총무(무지출 챌린지 서비스)의 웹 프론트엔드. 팀 거지야호가 원티드 AI Championship 2026 출품작으로 만든다. 과제 제출 마감은 2026-09-20이고, 심사 기간(9/21부터 10/5까지)에는 배포 링크가 항상 살아 있어야 한다.

## 늘 지킬 것

- 문서와 주석, 커밋 메시지, 응답은 한국어로 쓴다. 코드 식별자(변수, 함수, 타입)는 영어로 쓴다
- Git 규칙(브랜치, 커밋 형식, 금지 패턴)은 `.agents/rules/git-workflow.md`를 따른다
- 구현 후에는 `pnpm check`(타입 검사, 빌드, 린트, 포맷 검사)를 전부 통과해야 완료다
- 기능 요구사항이 코드와 이 문서에 없으면 추측으로 만들지 않고 사용자에게 묻는다

## 서비스 규칙 (확정된 것)

- 판결은 승인, 기각, 칭송 세 가지다. 승인은 회색, 기각은 빨강, 칭송은 초록
- 무지출 인증은 판결 없이 바로 칭송받는 별도 경로다
- 지출은 자진 신고 기반이다. 계좌와 카드 연동은 하지 않는다
- 랭킹은 무지출 랭킹(잘 참은 사람)과 거지왕 랭킹(많이 쓴 사람) 두 축이다
- 챌린지 강도는 순한맛, 매운맛, 지옥맛 3단계로 총무의 잔소리 수위를 정한다
- AI 연동은 에이전트가 OpenAI API 스타일로 응답하고 백엔드(geoji-server)가 받는다

서비스명과 도메인, 등급 체계, 총무 캐릭터 이름은 아직 미확정이다. 미확정 값을 코드에 박아야 하면 사용자에게 먼저 확인한다.

## 자주 쓰는 명령

| 명령            | 하는 일               |
| --------------- | --------------------- |
| `pnpm dev`      | 개발 서버 (포트 3800) |
| `pnpm check`    | 게이트 일괄 실행      |
| `pnpm lint:fix` | ESLint 자동 수정      |
| `pnpm format`   | Prettier 포맷         |

## 저장소 구조

- `src/` 애플리케이션 코드. 전역 스타일은 `src/shared/styles/globals.css`
- `docs/` 제품과 디자인, 운영 문서. 배치 기준은 `docs/CLAUDE.md`
- `.agents/` 에이전트 룰과 스킬의 본문. 구조는 `.agents/README.md` 참고
- `.github/workflows/` CI(`ci.yaml`)와 GitHub Pages 배포(`deploy.yaml`)
